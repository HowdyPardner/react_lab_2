    
//4. Fetch data and display (Hard)
    // 1. Make a Users and User components
    // 2. in Users a useEffect, fetch 'https://jsonplaceholder.typicode.com/users' (array of user objects)
    // 3. map through the array and for each user create a User instance (component) using that specific user's data